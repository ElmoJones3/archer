interface:
  display_name: "Test-Driven Development"
  short_description: "Observe the intended failure before implementation"
  default_prompt: "Use $principle-test-tdd to prove this behavior red, green, then safe to refactor."
policy:
  allow_implicit_invocation: true
