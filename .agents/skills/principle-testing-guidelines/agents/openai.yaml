interface:
  display_name: "Testing Guidelines"
  short_description: "Route every behavior to the proof it needs"
  default_prompt: "Use $principle-testing-guidelines to choose and apply the proof this behavior requires."
policy:
  allow_implicit_invocation: true
