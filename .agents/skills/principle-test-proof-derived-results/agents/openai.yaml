interface:
  display_name: "Derived Result Proofs"
  short_description: "Prove models produce faithful derived results"
  default_prompt: "Use $principle-test-proof-derived-results to prove what this behavior-bearing model produces."
policy:
  allow_implicit_invocation: true
